//The following code has been commented out because it was causing a
//recurring JavaScript error in the console,logging
//"Uncaught TypeError: console.logger is not a function" every 10 seconds.

// function oopsie() {
//   setTimeout(oopsie, 10000); // Keeps calling itself every 10 seconds
//   console.log("just saying hello - is this thing on?"); // Corrected method to console.log for future reference
//   return true;
// }

// setTimeout(oopsie, 3000); // Initial call after 3 seconds
